apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: frontend-rollout
  namespace: {{ .Values.namespace.name | default .Values.global.namespace }}
  labels:
    {{- include "bluegreen-deploy.frontend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.frontend.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: frontend
      app.kubernetes.io/instance: {{ .Release.Name }}
  strategy:
    {{- if eq .Values.frontend.rollout.strategy "blueGreen" }}
    blueGreen:
      activeService: {{ .Values.frontend.rollout.activeService }}
      previewService: {{ .Values.frontend.rollout.previewService }}
      autoPromotionEnabled: {{ .Values.frontend.rollout.autoPromotionEnabled }}
      scaleDownDelaySeconds: {{ .Values.frontend.rollout.scaleDownDelaySeconds }}
      prePromotionAnalysis:
        templates:
        - templateName: success-rate
        args:
        - name: service-name
          value: {{ .Values.frontend.rollout.previewService }}
      postPromotionAnalysis:
        templates:
        - templateName: success-rate
        args:
        - name: service-name
          value: {{ .Values.frontend.rollout.activeService }}
    {{- end }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: frontend
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      containers:
      - name: frontend
        image: "{{ .Values.frontend.image.repository }}:{{ .Values.frontend.image.tag }}"
        imagePullPolicy: {{ .Values.frontend.image.pullPolicy }}
        ports:
        - containerPort: {{ .Values.frontend.service.targetPort }}
          name: http
          protocol: TCP
        env:
        {{- range .Values.frontend.env }}
        - name: {{ .name }}
          value: {{ .value | quote }}
        {{- end }}
        resources:
          {{- toYaml .Values.frontend.resources | nindent 10 }}
